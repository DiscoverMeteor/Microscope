import { createContainer } from 'meteor/react-meteor-data';

import PostsNew from '../components/PostsNew.jsx';

const PostsNewPage = createContainer(() => {
  return {
  };
}, PostsNew);

export default PostsNewPage;
